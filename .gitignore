.env
.env.local
node_modules/
dist/
tsconfig.tsbuildinfo
.DS_Store
sprout-track/

# Heavyweight mockup art kept local-only (design reference lives in docs/mockups)
docs/mockups/v1-storybook/art/family-porch-flipped.png

# Firebase config artifacts (real files come from the Firebase console; not checked in)
android/app/google-services.json
# Not used on iOS - iOS push is direct APNs with no Firebase iOS SDK (spec
# decision D2, Package.swift carries no Firebase dependency). Ignored only so
# an operator who mistakenly drops one in doesn't accidentally commit it.
ios/App/App/GoogleService-Info.plist

# Browser APKs for emulator testing (download from apkmirror.com), plus the
# manifest.json that scripts/install-test-browsers.mjs generates to track them.
# Hundreds of MB each - never checked in; see CLAUDE.md.
apks/
